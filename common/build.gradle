plugins {
    id 'java-library'
}

group = rootProject.maven_group
version = rootProject.mod_version

base {
    archivesName = 'brokenstarsmp-common'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release = 25
}
