plugins {
    id 'java-library'
}

group = rootProject.maven_group
version = rootProject.mod_version

base {
    archivesName = 'brokenstarsmp-spigot'
}

dependencies {
    compileOnly "org.spigotmc:spigot-api:${rootProject.spigot_api_version}"
}
